import {IPv4Options} from "./model";

export function ipv4Validator(targetIp: string, options: IPv4Options): boolean;
export function ipv6Validator(targetIp: string): boolean;
export function ipValidator(targetIp: string): boolean;